---
id: draft-20260513-103507
title: Repo-Policy Shared Workspace Paths for Managed jx Workspaces
status: draft
created: 2026-05-13
updated: 2026-05-13
currentPhase: 1
externalRef: 
origin: 
---

# Repo-Policy Shared Workspace Paths for Managed jx Workspaces

## Outcome

Use `workspace_shared_paths` as the repo-policy name, and make the setup atomic from the operator’s point of view.

The important design shift is: validate shared-path setup before `jj workspace add` creates anything. If a required shared path cannot be created, `jx work add` should fail with no workspace registered and no directory to repair. Rollback remains a safety net for unexpected post-create failures, not the normal repair path.

```toml
[repo]
workspace_shared_paths = [".pi"]

[[repo.rules]]
repo = "example-owner/example-repo"
workspace_shared_paths = [".pi", ".foo/bar/baz"]
```

All entries are required in the first version. Optional shared paths can be a future richer shape if needed.

## Phases

- [ ] 1. Repo-policy shared path surface
- [ ] 2. Work-add planning from the primary checkout
- [ ] 3. Shared-path preflight before workspace creation
- [ ] 4. Conservative symlink application boundary
- [ ] 5. Success commit point and shell cd
- [ ] 6. preflight passes,
- [ ] 7. jj workspace creation succeeds,
- [ ] 8. metadata is written when needed,
- [ ] 9. shared paths are linked.
- [ ] 10. Docs and tests

## Phase Details

### Phase 1: Repo-policy shared path surface

Add `workspace_shared_paths` to base repo policy and matching repo rules.

The effective list composes in declaration order. Exact duplicate paths dedupe after normalization, while parent/child overlaps are invalid config because one shared path would make the other ambiguous. Validation also rejects empty paths, absolute paths, `..`, and paths that escape the repo-relative namespace.

### Phase 2: Work-add planning from the primary checkout

`jx work add` should build an explicit plan before mutating anything:

- repository identity,
- primary checkout root,
- destination workspace root,
- selected target revision,
- task metadata intent,
- effective `workspace_shared_paths`.

The primary checkout is always the source of truth, even when `jx work add` runs inside an existing managed workspace.

### Phase 3: Shared-path preflight before workspace creation

Preflight should reject deterministic failures before `jj workspace add`:

- source path missing in the primary checkout,
- destination workspace path already exists,
- any configured shared path is tracked in the target revision.

The target-revision contract is simple: shared paths must be local-only. If a path is tracked, it will already be present in every workspace and should not be configured as shared. For nested paths such as `.foo/bar/baz`, only that configured tail path must be untracked; parent directories may be tracked directories and can still contain the symlinked tail.

This is the repairability boundary: normal config/filesystem errors leave nothing behind, so the operator can fix and rerun.

### Phase 4: Conservative symlink application boundary

After successful preflight and workspace creation, apply symlinks in one filesystem boundary:

- create destination parent directories,
- symlink `workspace/path -> primary/path`,
- fail on unexpected existing destination.

If an unexpected post-create failure happens, `jx` should best-effort forget and remove the workspace, then report the original setup error plus cleanup failure if rollback also fails.

### Phase 5: Success commit point and shell cd

`jx work add` should only report success and emit hidden shell cd output after:

### Phase 6: preflight passes,

### Phase 7: jj workspace creation succeeds,

### Phase 8: metadata is written when needed,

### Phase 9: shared paths are linked.

Shell integration should only `pushd` after that full setup point, so it never lands the operator in a workspace missing required local config.

### Phase 10: Docs and tests

Document `workspace_shared_paths` under repo policy. Tests should cover policy composition, path validation including overlap rejection, `.pi` directory sharing, nested parent creation, missing-source preflight failure, tracked-shared-path preflight failure, rollback fallback, and shell cd output only after complete setup.

## Plan Notes

## Summary

Use `workspace_shared_paths` as the repo-policy name, and make the setup atomic from the operator’s point of view.

The important design shift is: validate shared-path setup before `jj workspace add` creates anything. If a required shared path cannot be created, `jx work add` should fail with no workspace registered and no directory to repair. Rollback remains a safety net for unexpected post-create failures, not the normal repair path.

```toml
[repo]
workspace_shared_paths = [".pi"]

[[repo.rules]]
repo = "example-owner/example-repo"
workspace_shared_paths = [".pi", ".foo/bar/baz"]
```

All entries are required in the first version. Optional shared paths can be a future richer shape if needed.

## Implementation details

### Phase 1 — Repo-policy shared path surface

Add `workspace_shared_paths` to base repo policy and matching repo rules.

The effective list composes in declaration order. Exact duplicate paths dedupe after normalization, while parent/child overlaps are invalid config because one shared path would make the other ambiguous. Validation also rejects empty paths, absolute paths, `..`, and paths that escape the repo-relative namespace.

### Phase 2 — Work-add planning from the primary checkout

`jx work add` should build an explicit plan before mutating anything:

- repository identity,
- primary checkout root,
- destination workspace root,
- selected target revision,
- task metadata intent,
- effective `workspace_shared_paths`.

The primary checkout is always the source of truth, even when `jx work add` runs inside an existing managed workspace.

### Phase 3 — Shared-path preflight before workspace creation

Preflight should reject deterministic failures before `jj workspace add`:

- source path missing in the primary checkout,
- destination workspace path already exists,
- any configured shared path is tracked in the target revision.

The target-revision contract is simple: shared paths must be local-only. If a path is tracked, it will already be present in every workspace and should not be configured as shared. For nested paths such as `.foo/bar/baz`, only that configured tail path must be untracked; parent directories may be tracked directories and can still contain the symlinked tail.

This is the repairability boundary: normal config/filesystem errors leave nothing behind, so the operator can fix and rerun.

### Phase 4 — Conservative symlink application boundary

After successful preflight and workspace creation, apply symlinks in one filesystem boundary:

- create destination parent directories,
- symlink `workspace/path -> primary/path`,
- fail on unexpected existing destination.

If an unexpected post-create failure happens, `jx` should best-effort forget and remove the workspace, then report the original setup error plus cleanup failure if rollback also fails.

### Phase 5 — Success commit point and shell cd

`jx work add` should only report success and emit hidden shell cd output after:

1. preflight passes,
2. jj workspace creation succeeds,
3. metadata is written when needed,
4. shared paths are linked.

Shell integration should only `pushd` after that full setup point, so it never lands the operator in a workspace missing required local config.

### Phase 6 — Docs and tests

Document `workspace_shared_paths` under repo policy. Tests should cover policy composition, path validation including overlap rejection, `.pi` directory sharing, nested parent creation, missing-source preflight failure, tracked-shared-path preflight failure, rollback fallback, and shell cd output only after complete setup.

## Working Log

### 2026-05-13 — Planning turn
Request:
  explain these three checkes:

   - shared path would collide with content from the target revision,
   - nested shared path needs a parent that would be a checked-out file,
   - duplicate or overlapping configured paths create ambiguous results
Response:
  These checks are about avoiding a workspace that is created successfully but cannot be prepared with the required shared paths.

  1. `shared path would collide with content from the target revision`

  `jj workspace add` checks out a revision into the new workspace. If that revision already contains the configured shared path, we cannot also place a symlink there.

  Example:

  ```toml
  workspace_shared_paths = [".pi"]
  ```

  But the target revision has a tracked `.pi` file or directory. After checkout, `workspace/.pi` already exists, so `workspace/.pi -> primary/.pi` cannot be created safely.

  Preflight should catch this before creating the workspace.

  2. `nested shared path needs a parent that would be a checked-out file`

  For a nested shared path, `jx` may need to create parent directories in the workspace.

  Example:

  ```toml
  workspace_shared_paths = [".foo/bar/baz"]
  ```

  To symlink `baz`, `jx` needs:

  ```text
  workspace/.foo/bar/
  ```

  If the target revision has `.foo` as a file, or `.foo/bar` as a file, then `jx` cannot create the parent directory structure. That is different from the final symlink path colliding; the blocker is one of the parents.

  So these should fail preflight:

  ```text
  target revision contains .foo          # file
  target revision contains .foo/bar      # file
  ```

  3. `duplicate or overlapping configured paths create ambiguous results`

  Exact duplicates are easy: normalize and dedupe.

  ```toml
  workspace_shared_paths = [".pi", "./.pi"]
  ```

  Both mean `.pi`, so keep one.

  Overlaps are more dangerous:

  ```toml
  workspace_shared_paths = [".pi", ".pi/settings.toml"]
  ```

  If `.pi` is symlinked as a directory, then `.pi/settings.toml` is already inside that symlinked tree. Creating another symlink inside it is either impossible, redundant, or points into the primary checkout in a surprising way.

  Another example:

  ```toml
  workspace_shared_paths = [".foo", ".foo/bar/baz"]
  ```

  If `workspace/.foo` becomes a symlink, then `workspace/.foo/bar/baz` is no longer a normal workspace path to create.

  I’d treat prefix overlaps as invalid for the first version. Exact duplicates dedupe; parent/child overlaps fail with a clear config error. That keeps behavior easy to reason about.

### 2026-05-13 — Planning turn
Request:
  so 1 means the contract should be shared paths must not be tracked, otherwise they'd be part of the workspace alraedy, we should verify that as a precondition, instead of the other stuff. for nested shared path we only need to ensure that the tail (ie linked thinng) is not tracked, it's very similar to the first check. duplicates or overlapping should just be precondition config validation
Response:
  Yes — that’s the cleaner contract.

  The preflight should not reason about every parent shape in the checkout. It should enforce the simple invariant:

  `workspace_shared_paths` are local-only paths. A configured shared path must not be tracked at the target revision.

  So:

  - `.pi` must not be tracked.
  - `.foo/bar/baz` must not be tracked.
  - `.foo` and `.foo/bar` may be tracked directories; that’s fine because we can create/link the tail inside them.
  - If `.foo` or `.foo/bar` is a tracked file, symlink application will still fail, but that’s an ordinary filesystem/setup failure rather than a special preflight rule. We can later add a nicer diagnostic if it matters.

  For config validation:

  - Exact duplicates after normalization should dedupe.
  - Parent/child overlaps should be invalid config:
    - `.pi` + `.pi/settings.toml`
    - `.foo` + `.foo/bar/baz`

  That belongs in repo-policy validation because it is independent of any target revision.

  I updated the durable plan in that direction: Phase 1 now treats overlaps as config validation, and Phase 3 now uses the “shared paths must be untracked” preflight contract.

### 2026-05-13 — Planning turn
Request:
  are there any unresolved questions?
Response:
  A few are still worth deciding before implementation:

  - **How to represent optional paths later.** First version says all entries are required. That’s good. But if you expect global defaults like `.pi` across repos where not all have `.pi`, we’ll need either per-repo rules only or a future richer shape (`{ path, required = false }`). No need to solve now unless you want global defaults immediately.

  - **What “tracked at target revision” exactly means at the jj boundary.** I think it should mean: the configured path resolves to a tree value in the selected commit’s tree. For `.foo/bar/baz`, only `baz` at that exact path is disallowed. Parent dirs being tracked dirs are okay.

  - **How to handle symlink source type.** I’d allow source files, directories, and symlinks; create a symlink to whatever exists at `primary/path`. If the source itself is a symlink, the workspace link points to that symlink path, not its resolved target. That preserves “primary checkout is source of truth.”

  - **Rollback semantics after `jj workspace add` succeeds.** Plan says best-effort forget/remove on post-create failure. We should decide if rollback is in the same filesystem boundary or a command-level compensating action. I’d keep it command-level because it must coordinate jj workspace registry + directory cleanup.

  - **Diagnostic shape.** Worth choosing clear errors up front:
    - missing source: “Shared workspace path `.pi` does not exist in primary checkout”
    - tracked target: “Shared workspace path `.pi` is tracked at the selected revision; remove it from config or untrack it”
    - overlap: “Shared workspace paths `.pi` and `.pi/settings.toml` overlap”

  Everything else seems settled enough to implement.
